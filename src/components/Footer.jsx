function Footer() {
  return (
    <footer className="app-footer">
      <span>Frontend layout ready for role management workflows.</span>
      <span>Header and footer are shared across every page.</span>
    </footer>
  )
}

export default Footer
